extends ../_layout.jade
block content
    if currentUser && currentUser.id == user.id
        a.pull-right(href="/users/edit") Edit your profile

    h1= user.name
    h2 Joined on #{ user.createdat }

    if user.bio
        p= user.bio